# Live QR Code Scanner (Python + OpenCV + Pyzbar)

A simple live QR code scanner using your webcam.
Press `q` to exit.

## Requirements
- Python 3.8+
- OpenCV
- Pyzbar
- Numpy

## Install
```bash
pip install -r requirements.txt

```
## Run
```bash
python Scanner.py